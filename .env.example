# Autonomous Earnings Scout - Environment Variables
# Copy this file to .env and configure as needed

# Claude Agent SDK (uses Claude Code CLI authentication - no API key needed)
# The SDK automatically uses your existing Claude Code session

# Optional: Override default model
# MODEL=claude-sonnet-4-5

# Optional: Set output directory
# OUTPUT_DIR=./output

# Optional: Investment preferences (can also be set via CLI args)
# DEFAULT_SECTORS=ai_ml,cloud_saas,aws_ecosystem
# DEFAULT_MIN_SCORE=75
# DEFAULT_DAYS_AHEAD=60
